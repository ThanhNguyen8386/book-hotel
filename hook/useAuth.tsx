// import useSWR from "swr";
// import { fetchUser } from "@/lib/api";

// export const useAuth = () => {
//     const { data: user, mutate } = useSWR("/me", fetchUser);
//     return { user, mutate };
// };